export class GreedyBEFS {
    constructor() {
        this.time_elapsed = 0;
    }

    findPath(grid, start, end) {
        
    }

    drawCell([cell, color, alternativeBorderColor, cellBorderColor, pathColor]) {
        return [color, cellBorderColor]
    }

}